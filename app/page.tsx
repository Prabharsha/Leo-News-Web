import { catogories } from "@/constant"
import fetchNews from "@/lib/fetchNews";

export default async function Home() {
  // const news: NewsResponse = await  fetchNews(catogories.join(','));
  return (
    <div className=''>
      {/**NewsList news */}
    </div>
  )
}
